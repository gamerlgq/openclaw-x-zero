# 2026-05-07

- 排查 OpenClaw / Codex OAuth 401 `refresh_token_reused`：根因高度疑似为独立 Codex CLI (`~/.codex`, 全局 `@openai/codex`) 与 OpenClaw agent `main` 共用同一 OpenAI/Codex 账号，导致 refresh token 轮换互踩。
- 已按用户要求移除独立 Codex CLI：卸载全局 npm 包 `@openai/codex`；最初曾将 `~/.codex` 迁移到备份目录，随后按用户要求已删除该备份目录，不再保留独立 Codex CLI 本地状态。
- 保留 OpenClaw 内置 Codex 相关目录：`~/.openclaw/agents/main/agent/codex-home`、`~/.openclaw/extensions/codex`，未改动。
